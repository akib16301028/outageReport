streamlit==1.25.0  # Ensure compatibility with the version you're using
selenium==4.12.0   # Latest stable version of Selenium as of now
webdriver-manager==4.0.0  # Manages browser drivers like ChromeDriver automatically
